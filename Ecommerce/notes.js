// auth - signin, signup (role based)
// admin -> admin panel (add,edit,delete,active)
// user -> user panel -> view,add to cart, buy, profile

// Schema -> structure of data

// user -> name,email,password,phone, address, cart, orders

// admin -> name,email,password, products, orders, carts

